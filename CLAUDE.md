# CLAUDE.md

**Canonical instructions live in [PROTO.md](./PROTO.md).** Read it before editing — it
owns the stack, the lint+test gate, the per-agent gating design (**§4, don't break
it**), what to build (the stubs, §5), and the rules (§6, incl. host-free imports and
plain-string `@tool` docstrings). This file is a thin pointer; edit PROTO.md.
